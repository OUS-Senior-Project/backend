name: Backend CI (temporary)

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  lint_and_test_backend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # - name: Set up Python
      #   uses: actions/setup-python@v5
      #   with:
      #     python-version: '3.11'

      # - name: Install dependencies
      #   run: |
      #     pip install -r requirements.txt

      # - name: Lint backend
      #   run: |
      #     flake8 .

      # - name: Run tests
      #   run: |
      #     pytest

      - name: Temporary success step
        run: echo "✅ Backend CI placeholder passed successfully!"
